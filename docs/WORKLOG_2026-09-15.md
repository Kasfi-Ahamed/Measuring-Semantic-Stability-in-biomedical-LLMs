# Worklog — 15 September 2026

Login-node only. **Nothing released.** `30955`, `32769`, `32853`, `33061`, `33062` all remain
held.

---

## 1. Staleness sweep — CADEC and QA

`scripts/staleness_sweep_cadec_qa.py`. The dependency graph is **declared**, not inferred, so a
missing edge is a visible omission rather than a silent pass.

**36 artefacts checked: 32 CURRENT, 4 STALE, 0 missing.**

The suspicion was correct — the correction did leave things behind. Each is diagnosed below
rather than fixed, per the stop condition.

| artefact | built | newest input | verdict |
|---|---|---|---|
| `docs/AMENDMENT7_BEFORE_AFTER.md` | 15 Sep 18:24 | 19:25 | **STALE AND WRONG** |
| `docs/RQ4_CADEC_results.md` | 14 Sep 16:20 | 15 Sep 18:22 | stale, content identical |
| `outputs/rq3/tables/threshold_band_rows.csv` | 14 Sep 17:08 | 15 Sep 18:01 | stale, content identical |
| `outputs/rq3/tables/threshold_band_summary.json` | 14 Sep 17:08 | 15 Sep 18:01 | stale, content identical |

### The one that matters: `AMENDMENT7_BEFORE_AFTER.md`

**This is the document handed over to refresh six manuscript sections, and its RQ1 rows are
wrong.** It was generated at 18:24, between applying Amendment 7 and enforcing Amendment 8, so
its "after" column for RQ1 part 2 holds the **MixedLM** coefficients that Amendment 8 has since
reversed:

| term | doc says "after" | actually current (OLS) |
|---|---:|---:|
| `share_back_translation` | +0.0412 | **+0.6684** |
| `n_accepted_perts_z` | −0.0317 | **−0.3261** |

Its whole "RQ1's magnitude half changed ESTIMATOR" section describes a switch that no longer
exists. Every non-RQ1 row in it is still correct. **Not regenerated — flagged for the decision
on whether to regenerate or delete it now that Amendment 8 has settled the question.**

### The other three: mechanically stale, substantively correct

- **`RQ4_CADEC_results.md`** — its tables were rebuilt from pre-correction CSVs, but RQ4 moved
  by ~1e-5. Spot-checked BERT-base: doc says `0.4596 / 0.4648 / 0.7313 / 0.5351`, current is
  `0.45965 / 0.46482 / 0.73134 / 0.53506` — identical at the 4 decimal places it prints. Win
  counts identical at 0 / 5 / 8. Only its provenance line is out of date.
- **threshold band ×2** — verified read-only, **26 rows / 16 instances / nearest 1.313e-4 both
  before and after Amendment 7.** The 82 patched rows sit at distance 0.29 from the cut before
  and 0.70 after; a 5.95e-4 band never contained them. **S2's numbers stand.**

### QA is entirely current

All 11 QA artefacts CURRENT. The CADEC correction did not touch the QA lane, as expected.

---

## 2. `docs/SUPPLEMENTARY.md` refreshed

Every entry now names the post-correction artefact and its regeneration time. S1, S3, S4 and
S10 marked final under Amendments 7 and 8; S2 kept PROVISIONAL with the reason narrowed —
the threshold-band half is **verified unchanged**, only the tie-break count is still pre-remap.
No supplementary table references a `_PREEMPTYFIX` file, and **none of the four stale artefacts
backs a supplementary table**.

Two clarifications added where a reader could otherwise be misled:
- **S1**: both arms are OLS and *were* OLS on 14 September, so the arm comparison is
  like-for-like and the count is **unchanged at 6 of 28, the same six terms**. Three of the six
  are the intercept and two model fixed effects.
- **S4**: part 2 is enforced OLS on logit(H), 14 terms; the `Group Var` row is gone.

---

## 3. Runbook updated

- **New step 5a**, named explicitly because it is what makes the day survivable: CADEC entropy
  recomputes **on CPU in ~3 minutes with no slot**, because `CADEC_entropy.ipynb` now decides
  cache reuse in cell 3 before the FAISS index and SapBERT load. `CADEC_FORCE_REMAP=1`
  overrides.
- **New section 4c**: Amendment 7 applies to the cutoff re-map, with `MM_EMPTY_POLICY` required
  and no default, both production jobs on `unassigned`, and `replicate` marked as never valid
  for production. Expected scale on MedMentions: 639 empty rows plus 49 in unmapped shards.
- Amendment 8 recorded as applying to MedMentions RQ1, which matters because that data is new:
  under the old code whether part 2 reported logit(H) or H would have depended on an optimiser
  meeting a threshold on data nobody had seen.

---

## 4. Figures confirmed

All six CURRENT against post-correction inputs. The three CADEC figures were rebuilt after the
correction (15 Sep 18:10, risk-coverage re-verified 20:43); the three QA figures date from
14 Sep 16:20 and are current because the QA lane was untouched.

`fig_risk_coverage.py` prints, on every run:

```
AURC estimator: trapezoid over coverage [0.10, 1.00] on a 19-point grid (step 0.05),
NOT normalised by domain width.
Identical to RQ4_margin_benchmark.ipynb; the plotted curve is finer than the grid.
```

FLAN-T5-base entropy reads **0.6887**, matching the benchmark's 0.68872. The per-instance
integral is not used for any reported number.

---

## 5. Resolved — all four stale artefacts

**Decision: supersede, do not delete.** `docs/AMENDMENT7_BEFORE_AFTER.md` is retained with a
dated SUPERSEDED banner recording that its RQ1 part 2 section described a MixedLM switch
Amendment 8 has since reversed, that its other rows were correct, and that **the manuscript was
refreshed from the enforced-OLS table, not from it** — Results 4.1 reads -0.326, +0.668 and
+0.144, which are the current values. Nothing wrong reached the paper.

`docs/AMENDMENT7_8_BEFORE_AFTER.md` replaces it, generated by
`scripts/amendment78_before_after.py` and covering both amendments as one before/after.

**With both sides on OLS the RQ1 comparison is finally like-for-like, and 0 of 28 terms change
significance** — the "3 of 28" reported earlier today was the estimator switch, not the data.
Largest movement anywhere is RQ3 pair 2's rank-biserial, +0.1654 -> +0.1575, from 4 tied pairs
resolving; its interpretation is unchanged.

The other three were regenerated for provenance. The threshold band reproduces **exactly**:
26 rows, 16 instances, nearest 1.313e-4. `RQ4_CADEC_results.md` rebuilt from the current CSVs.

**Sweep re-run: 36 of 36 CURRENT, 0 stale.**

## Queue

Unchanged. `31879_[13,14,16]` pending, two tasks running at throttle 2, inference clearing
~17 Sep 03:00. Held: `30955`, `32769`, `32853`, `33061`, `33062`.
