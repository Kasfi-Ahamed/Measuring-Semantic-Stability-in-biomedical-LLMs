# Worklog — 14 September 2026

Eight-hour unattended work plan. Items 1-4 of the plan were already complete when it was
issued (commits `cffb180`, `410164a`, `feecb48`); this records them briefly and the new work
in full.

---

## COMPLETED

### 1-4. Sweep, collinearity, RQ1 output, zero-inflation table (already done)

- **Broad-except sweep** — `scripts/broad_except_sweep.py`. **99 handlers**, **zero bare
  `except:`**; all 99 are `except Exception:`. 17 could reach a reported number.
- **Collinearity** — RQ1 CADEC, both parts: **rank 14/14**, scaled condition number **27.4**
  and **27.5**, **max VIF 3.69**. The aliased design was rank 14 of 15, condition number
  6e+16. Nothing else is near-collinear.
- **RQ1 CADEC full output** — `docs/RQ1_CADEC_results.md`.
- **Zero-inflation table** — `docs/rq123_audit.md`, both denominators, regenerated on
  remapped data.

### 5. Cutoff runbook — `docs/CUTOFF_RUNBOOK.md` (`87608e6`)

Ordered sequence with times measured from `sacct`: **~8 min fixed overhead, ~2 h for the first
block, ~1.2 h per block after**, so **6-10 h for a full clean re-map** and **9-14 h** for the
whole day. Start the re-map no later than 08:00.

The finding that makes the day feasible: the shard CSVs for blocks 0-5 and 19 were pruned, so
those blocks cannot be re-mapped from source — but `rq1_all_outputs_mapped.csv` **retains
`output_text`** at 99.96-99.99% per block, so **the re-map needs no re-inference**. The runner
that reads the mapped file instead of the assembled shards is the one piece of code that must
be written before the 21st; validate it by reproducing block 6's known-clean mapping.

### 7. QA margin harness (`e3ebe6c`) — queued as 32770

`scripts/exec_notebook_cells.py` runs a named subset of cells so the QA half (cell 5) runs
while cell 4, which writes a MedMentions artefact, stays unexecuted. The launcher stats the
MedMentions margin file before and after and fails if its mtime moved.

Cell 5 could not run alone: its final `del` referenced `uniq_vecs`, `D50` and `I50`, which are
cell 4 locals, so the QA half raised `NameError` **after** writing its CSV. Fixed.

### 8. RQ4 CADEC rehearsal — COMPLETE, and it settles contribution 3

`scripts/rq4_bootstrap_calibrate.py`. Reuses the notebook's own `rank_orders` / `aurc_of` /
`selective_curve` rather than reimplementing AURC.

**Calibration.** 4.35 ms per bootstrap iteration at n = 4,712. Fit:
`seconds = 4.354 ms x B + 0.02 s`. Extrapolation to **B = 20,000: 1.5 minutes** for one cell.
Far under the 2-hour budget, so the full B was run: **all 8 CADEC models, 729.6 s total.**

**AURC, CADEC x FLAN-T5-base** (lower is better): entropy **0.68872**, combined 0.69596,
combined_3 0.70441, margin 0.72030, confidence 0.74196.

**Does combined_3 beat best_single? No — in 0 of 8 CADEC models,** and significantly not:

| model | best_single | AURC bs | AURC c3 | delta | 95% CI | p (pre-reg) |
|---|---|---:|---:|---:|:---:|---:|
| BERT-base | entropy | 0.45965 | 0.53506 | +0.07542 | [+0.06493, +0.08458] | 1.0e-4 |
| BioBERT | entropy | 0.32913 | 0.34284 | +0.01371 | [+0.00044, +0.02041] | 0.0419 |
| PubMedBERT | confidence | 0.32656 | 0.34201 | +0.01545 | [+0.00552, +0.02251] | 0.0013 |
| FLAN-T5-base | entropy | 0.68872 | 0.70441 | +0.01569 | [+0.00406, +0.02025] | 0.0040 |
| BioMistral-7B | entropy | 0.68928 | 0.71092 | +0.02164 | [+0.01523, +0.03161] | 1.0e-4 |
| Mistral-7B-Instruct-v0.1 | entropy | 0.74252 | 0.76794 | +0.02542 | [+0.01693, +0.03195] | 1.0e-4 |
| Llama3-OpenBioLLM-8B | entropy | 0.79201 | 0.81483 | +0.02282 | [+0.01665, +0.02838] | 1.0e-4 |
| Meta-Llama-3-8B-Instruct | entropy | 0.76611 | 0.77676 | +0.01065 | [+0.00347, +0.01728] | 0.0022 |

Every delta is **positive** (worse) and every CI **excludes zero on the wrong side**. This is
not "combined_3 fails to help"; it is "combined_3 is reliably worse than the best single
signal", and the best single signal is **entropy** in 7 of 8 models. Adding the margin to
entropy makes selective prediction worse.

**Which signal does the work: entropy.** It is best_single in 7 of 8; confidence takes
PubMedBERT. Margin is never best_single anywhere.

**Risk at the coverage levels anyone would actually abstain at** — the number worth reading,
since AURC integrates over coverages nobody uses:

| model | signal | 90% | 75% | 50% |
|---|---|---:|---:|---:|
| BioBERT | entropy | 0.5110 | 0.4177 | **0.3094** |
| BioBERT | combined_3 | 0.5121 | 0.4335 | 0.3340 |
| PubMedBERT | entropy | 0.5143 | 0.4211 | **0.3081** |
| FLAN-T5-base | entropy | 0.8095 | 0.7949 | 0.7585 |
| FLAN-T5-base | combined_3 | 0.8104 | 0.7960 | 0.7907 |
| Llama3-OpenBioLLM-8B | entropy | 0.9168 | 0.9066 | 0.8820 |

At **90% coverage the two signals are indistinguishable to three decimal places** on every
model. The separation only appears at 50% coverage, and it always favours entropy alone. For
the generative models the operating points are poor in absolute terms: FLAN-T5-base still errs
on **76%** of retained cases after abstaining on half. The encoders are the only cells where
abstention buys anything real — BioBERT and PubMedBERT reach ~31% risk at 50% coverage.

**Discrepancy with the pre-registration, found and reported.** The notebook's `boot_p` is a
plain two-sided proportion and returns **exactly 0**. `docs/ANALYSIS_PRECOMMIT.md` commits to
**`(r + 1) / (B + 1)`**, whose floor at B = 20,000 is 1.0e-4. Both are now computed and
written side by side (`p_vs_best` and `p_vs_best_preregistered`). No conclusion changes, but
a reported `p = 0` would have been below the resolution the resample count supports, which is
exactly what the pre-registration rejected.

Written to **new CADEC-only paths**: `outputs/rq3/rq4_aurc_bootstrap_ci_cadec.csv`,
`rq4_combined3_wintest_cadec.csv`, `rq4_risk_coverage_operating_points_cadec.csv`. No
MedMentions artefact touched.

### 6. One-block validation re-map — COMPLETE, BOTH GATES PASS

Job `32768`, block 6, scratch path, **COMPLETED in 1:03:06**.

```
exact_match_inject: 0 rows (0.00%)
rule-1 branch split:
  exact_match_inject           0    0.00%
  exact_match            153,801   41.52%
  no_exact_match          77,454   20.91%
entropy stage    : reached (entropy_full_umls_VALIDATE_b6.csv)
entropy rows     : 57,224   [m_accepted, m_distinct, retained_m_distinct all present]
PASS: both gates clean
```

**The stop condition did not trigger.** MedMentions work is not halted.

Both defects are confirmed fixed in one run: the rule-1 gold leak does not reappear
(`exact_match_inject` 0.00%), and the `NameError` that killed 32750 **after** it had written
2,927,685 mapped rows is gone — the entropy stage was reached and emitted dual-m columns. A
mapped file alone would have proved nothing, which is why gate 2 exists.

Isolation verified afterwards: production `entropy_full_umls.csv` unchanged (13 Sep 12:29),
`rq1_all_outputs_mapped.csv` unchanged (14 Sep 04:12), block 6's 20 shard CSVs still present
(the prune did not run), and all four writes — including the mesh-vs-full figure and the
transitions table — landed in `outputs/scratch/validate_map/`.

**This supersedes the runbook's re-map estimate.** 370,428 rows in 63 minutes, less ~8 minutes
fixed overhead, is **~55 minutes marginal per block**, so **~7.5 hours for all 8 blocks**.
SapBERT embedding was 27 seconds of it; the cost is entirely the FAISS top-1000 search over
7,653,278 vectors. `docs/CUTOFF_RUNBOOK.md` updated.

### B. Broad-except fix decisions applied (`c3ba34f`)

Fail loudly: RQ1 logit fit and OLS fallback now **raise**. Fail closed: QA `gate_g1` and
`gate_g2` now **reject** when they cannot evaluate, logged and counted. `kendalltau` import is
a plain import again. Bootstrap drops counted with a **<= 1% failure-rate assertion** — and
there were **two** silent drops, not one: the refit `except` and an `abs(coef) <= 100` filter.
Permanent assertion added that no accepted QA variant is byte-identical to its original.

### 9. Figure producers on CADEC and QA — COMPLETE

Six figures: `fig_entropy_distribution`, `fig_signal_independence`, `fig_risk_coverage`, each
for CADEC and QA. The three QA figures came out **byte-identical** to the committed ones —
`qa_results_combined.csv` has not changed since 4 September and the producers are
deterministic. QA risk-coverage reads `qa_results_combined.csv`, not the margin file, so it
did not need job 32770.

Signal independence, CADEC: the three signals are **not** independent in the encoders
(PubMedBERT confidence vs entropy rho = -0.580, BioBERT -0.525, BERT-base -0.496) and close to
independent in the generative models (BioMistral -0.058, Mistral -0.063). That asymmetry is
worth a sentence: the premise for combining signals holds where combining helps least.

**A duplicate-estimator finding came out of this and is NOT fixed** — see
`docs/BUG_AUDIT.md`. `fig_risk_coverage.py` and `RQ4_margin_benchmark.ipynb` both compute
AURC and disagree: CADEC x FLAN-T5-base entropy is **0.75804** in the figure and **0.68872**
in every table. Isolated the cause rather than guessing it — grid resolution accounts for
0.00042; the rest is the **integration domain**, `1/n..1.00` against `0.10..1.00`, with
neither normalised by its own width. The figure therefore prints AURCs that contradict
`rq4_aurc_margin_benchmark.csv` and the RQ4 rehearsal above. Choosing between them changes
pre-registered numbers, so it is left as decision 4 below.

---

## STOPPED, AND WHY

### The `main.tex` placeholder mapping (runbook item 5, fourth bullet)

**There is no `main.tex` in this repository, and no `.tex` file anywhere under it or under
`$HOME` to depth 3.** The manuscript is not in this repo, so the placeholder names cannot be
read and I did not invent them. The runbook instead lists the artefact each Results section
consumes; paste the placeholder names against those rows and the mapping is done.

*(Item 6 finished after this section was first written; it is now under COMPLETED.)*

### Item A and item 7 — queued behind the QoS cap

`32773` (QA gate audit) and `32770` (QA margin) are both PENDING on
`QOSMaxJobsPerUserLimit`, behind `32768` and the running production shard `32009_17`. Neither
has produced a number. Item 9 (figures) needed no slot and is complete.

---

## DECISIONS DEFERRED TO YOU

1. **An auto-submitted `mm_partial_map` job appeared and is held.** Job `32769` was submitted
   at 10:02 from `g40-4gpu-2` by the epilogue of `run_mm_causal_shard_inf.sbatch`, which ends
   with `sbatch slurm/run_mm_partial_map.sbatch` unless one is already queued. It would have
   run RQ1_PART2 in production mode, written `entropy_full_umls.csv` from the **contaminated**
   mixed mapped file, and pruned block 6's shard CSVs. **I held it** (`scontrol hold`, never
   `scancel`). Holding it has a useful side effect: the guard is `squeue -h -n mm_partial_map`,
   so the held job now suppresses further auto-submissions from every pending shard job.
   **Do not release or delete it casually — either re-arms the auto-submit.** Whether to
   disable the auto-submit properly before the 21st is your call; I did not change the
   launcher.

2. **The MixedLM handler was not made to raise.** Instructed to fail loudly; it prints its
   traceback loudly but keeps the OLS fallback, because that fallback is the *designed* path
   and is already named in the reported method string. Re-raising would delete a documented
   analysis path rather than a silent failure. Reasoning in `docs/BUG_AUDIT.md`. Say the word
   and it becomes a raise like the other two.

3. **Which AURC is the paper's AURC?** Three options are laid out in `docs/BUG_AUDIT.md`.
   Normalising by domain width is the defensible one statistically, and it changes every AURC
   already computed and pre-registered, so it is not mine to take. Until it is settled,
   `fig_risk_coverage_*.png` should not go in the manuscript beside the AURC tables.

4. **Contribution 3 needs a decision, not more computation.** combined_3 is significantly
   worse than entropy alone in all 8 CADEC models, the margin is never the best single signal,
   and at 90% coverage no signal separates from any other. Combined with the zero-block AUROC
   of 0.5046 already recorded, CADEC does not support the candidate margin as a useful
   abstention signal. What the paper claims about it is yours to decide.

---

## CORRECTION TO MY OWN EARLIER REPORT

I told you "0 of 7,779 SQuAD2 and 0 of 1,484 BioASQ persisted variants are identical to their
original", and you reasonably built decision B's "KEEP, but assert" on it. **That is not what
I measured.** In `qa_question_perturbations_{dataset}.csv`, `pert_idx == 0` is the **first
accepted perturbation**, not the original — the original question is not in that file at all.
I compared perturbations against perturbation 0. The real statement is "no variant is
identical to variant 0", which is weaker and does **not** establish that the fail-open
operators never fired.

`scripts/qa_gate_failopen_audit.py` (job `32773`) recovers the true originals from the SQuAD
2.0 validation split and the BioASQ zip and measures the correct quantity alongside the gate
recomputation. The assertion added to the notebook compares against the original.

---

## Commits

| commit | what |
|---|---|
| `87608e6` | cutoff runbook |
| `e3ebe6c` | QA margin cell-5-only harness, cell 4/5 decoupled |
| `c3ba34f` | broad-except fix decisions, and the correction above |
| `d4ce455` | RQ4 CADEC rehearsal at B = 20,000 |
| `b59b377` | six figures, and the duplicate-AURC finding |
| `bad8534` | runbook re-map estimate replaced with the measured time |
