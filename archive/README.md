# archive/

Prior-run snapshots that are **not** licensed UMLS/CADEC dumps.

Pilot outputs that would otherwise live under `outputs/` can be copied here before a full corpus rerun. Large pre-rerun trees belong in `outputs_archive/` (gitignored) or compressed off-quota storage — do not commit raw generations.
